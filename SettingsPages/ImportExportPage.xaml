<Page
    x:Class="Fairmark.SettingsPages.ImportExportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Orientation="Vertical" Padding="20">
            <Grid Margin="0,0,0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Import vault" FontWeight="SemiBold" FontSize="30" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="Import all notes and tags from a previous vault. If an imported tag already exists, it will be skipped and its notes will be attached to the previous tag. If an imported note's name is not unique, it will have a suffix. Use a *.fmbkp file here." Opacity="0.7"/>
                <Button Margin="0,15,0,0" IsEnabled="False" Grid.Row="2" Content="Import vault" Style="{ThemeResource AccentButtonStyle}"/>
            </Grid>
            
            <Grid Margin="0,0,0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Import notes" FontWeight="SemiBold" FontSize="30" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="Import separate notes. Tags will not be imported. If an imported note's name is not unique, it will have a suffix. Use an *.fmnote file here." Opacity="0.7"/>
                <Button Margin="0,15,0,0" IsEnabled="False" Grid.Row="2" Content="Import notes" Style="{ThemeResource AccentButtonStyle}"/>
            </Grid>
            
            <Grid Margin="0,0,0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Export vault" FontWeight="SemiBold" FontSize="30" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="Export all notes and tags to to a *.fmbkp file here." Opacity="0.7"/>
                <Button Margin="0,15,0,0" IsEnabled="False" Grid.Row="2" Content="Export vault" Style="{ThemeResource AccentButtonStyle}"/>
            </Grid>
            
        </StackPanel>
    </ScrollViewer>
</Page>
