<Page
    x:Class="Fairmark.SettingsPages.ImportExportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Orientation="Vertical" Padding="20">
            <Grid Margin="0,0,0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Uid="ImportVaultHeader" FontWeight="SemiBold" FontSize="30" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" x:Uid="ImportVaultDesc" Opacity="0.7"/>
                <Button Margin="0,15,0,0" IsEnabled="True" Grid.Row="2" x:Uid="ImportVaultButton" Style="{ThemeResource AccentButtonStyle}" x:Name="ImportVault" Click="ImportVault_Click"/>
            </Grid>
            <Grid Margin="0,0,0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Uid="ExportVaultHeader" FontWeight="SemiBold" FontSize="30" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" x:Uid="ExportVaultDesc" Opacity="0.7"/>
                <Button Margin="0,15,0,0" IsEnabled="True" Grid.Row="2" x:Uid="ExportVaultButton" Style="{ThemeResource AccentButtonStyle}" x:Name="ExportVault" Click="ExportVault_Click"/>
            </Grid>
            
        </StackPanel>
    </ScrollViewer>
</Page>
