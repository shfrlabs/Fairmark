<Page
    x:Class="Fairmark.SettingsPages.AIPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:helpers="using:Fairmark.Helpers"
    xmlns:converters="using:Fairmark.Converters"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.Text"
    xmlns:intelligence="using:Fairmark.Intelligence" 
    mc:Ignorable="d">
    <ScrollViewer Padding="20">
        <StackPanel Orientation="Vertical">
            <TextBlock x:Uid="AIPage_Warning" Opacity="0.7" Margin="5" />

            <StackPanel Orientation="Vertical" DataContext="{x:Bind ais}">
                <toolkit:SettingsCard x:Uid="AIPage_EnableAI" IsEnabled="False">
                    <toolkit:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xF4A5;" />
                    </toolkit:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{Binding IsAIEnabled, Mode=TwoWay}" IsEnabled="False"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_SelectProvider" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}">
                    <ComboBox SelectedItem="{Binding SelectedProvider, Mode=TwoWay}" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}" ItemsSource="{Binding AvailableProviders, Mode=OneWay}"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_SelectModel" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}">
                    <ComboBox SelectedItem="{Binding SelectedModel, Mode=TwoWay}" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}" ItemsSource="{Binding AvailableModels, Mode=OneWay}" DisplayMemberPath="Description"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_ApiKey" DataContext="{x:Bind currentProvider, Mode=OneWay}" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}">
                    <TextBox x:Uid="AIPage_ApiKeyInput" Text="{Binding ApiKey, Mode=TwoWay}" MinWidth="250" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}" />
                </toolkit:SettingsCard>

                <toolkit:SettingsExpander x:Uid="AIPage_TestExpander">
                    <toolkit:SettingsExpander.Items>
                        <StackPanel Orientation="Vertical">
                            <TextBox x:Uid="AIPage_TestInput" x:Name="QueryBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}"/>
                            <Button x:Name="Send" x:Uid="AIPage_SendButton" HorizontalAlignment="Left" Click="Send_Click" Margin="0,10,0,0" IsEnabled="{x:Bind ais.IsAIEnabled, Mode=OneWay}"/>
                            <TextBlock x:Name="Response" TextWrapping="Wrap" Margin="0,10,0,0" />
                        </StackPanel>
                    </toolkit:SettingsExpander.Items>
                </toolkit:SettingsExpander>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>