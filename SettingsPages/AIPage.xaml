<Page
    x:Class="Fairmark.SettingsPages.AIPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:helpers="using:Fairmark.Helpers"
    xmlns:converters="using:Fairmark.Converters"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.Text"
    xmlns:intelligence="using:Fairmark.Intelligence" 
    mc:Ignorable="d">
    <ScrollViewer Padding="20">
        <StackPanel Orientation="Vertical">
            <TextBlock x:Uid="AIPage_Warning" Opacity="0.7" Margin="5" />

            <StackPanel Orientation="Vertical" DataContext="{x:Bind ais}">
                <toolkit:SettingsCard x:Uid="AIPage_EnableAI">
                    <toolkit:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xF4A5;" />
                    </toolkit:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{Binding IsAIEnabled, Mode=TwoWay}"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_SelectProvider">
                    <ComboBox SelectedItem="{Binding SelectedProvider, Mode=TwoWay}" ItemsSource="{Binding AvailableProviders, Mode=OneWay}"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_SelectModel">
                    <ComboBox SelectedItem="{Binding SelectedModel, Mode=TwoWay}" ItemsSource="{Binding AvailableModels, Mode=OneWay}" DisplayMemberPath="Description"/>
                </toolkit:SettingsCard>

                <toolkit:SettingsCard x:Uid="AIPage_ApiKey" DataContext="{x:Bind currentProvider, Mode=OneWay}">
                    <TextBox x:Uid="AIPage_ApiKeyInput" Text="{Binding ApiKey, Mode=TwoWay}" MinWidth="250" />
                </toolkit:SettingsCard>

                <toolkit:SettingsExpander x:Uid="AIPage_TestExpander">
                    <toolkit:SettingsExpander.Items>
                        <StackPanel Orientation="Vertical">
                            <TextBox x:Uid="AIPage_TestInput" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                            <Button x:Name="Send" x:Uid="AIPage_SendButton" HorizontalAlignment="Left" Click="Send_Click" Margin="0,10,0,0" />
                            <TextBlock x:Name="Response" TextWrapping="Wrap" Margin="0,10,0,0" />
                        </StackPanel>
                    </toolkit:SettingsExpander.Items>
                </toolkit:SettingsExpander>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>