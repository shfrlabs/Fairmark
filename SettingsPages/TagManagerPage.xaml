<Page
    x:Class="Fairmark.SettingsPages.TagManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:Fairmark.Helpers"
    xmlns:converters="using:Fairmark.Converters"
    xmlns:models="using:Fairmark.Models"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:TagGradientConverter x:Name="TagGradientConverter"/>
        <converters:ColorBrushConverter x:Name="ColorBrushConverter"/>
        <converters:ColorGradientConverter x:Name="ColorGradientConverter"/>
        <converters:NullBooleanConverter x:Name="NullBooleanConverter"/>
        <converters:CountVisibilityConverter x:Name="CountVisibilityConverter"/>
    </Page.Resources>
    <Grid Padding="20" MaxWidth="900">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{x:Bind helpers:NoteCollectionHelper.tags}" SelectionChanged="ListView_SelectionChanged" Grid.Column="0" x:Name="TagList">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:NoteTag">
                    <Grid Margin="-12, 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Background="{x:Bind Color, Converter={StaticResource ColorGradientConverter}, Mode=OneWay}" Margin="0, -6" CornerRadius="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="0" Grid.ColumnSpan="2"/>
                        <TextBlock Text="{x:Bind Emoji, Mode=OneWay}" FontFamily="Segoe UI Emoji" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="1" Margin="50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button x:Name="RenameTag" Content="Rename tag" Click="RenameTag_Click" Grid.Row="0" Margin="5" Width="150"/> 
            <Button x:Name="DeleteTag" Content="Delete tag" Click="DeleteTag_Click" Grid.Row="1" Margin="5" Width="150"/> 
            <Button x:Name="RecolorTag" Content="Change tag color" Click="RecolorTag_Click" Grid.Row="2" Margin="5" Width="150"/> 
            <Button x:Name="EmojiTag" Content="Change tag emoji" Click="EmojiTag_Click" Grid.Row="3" Margin="5" Width="150"/> 
        </Grid>
    </Grid>
</Page>
