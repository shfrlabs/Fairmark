<Page
    x:Class="Fairmark.FileEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:oldtoolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <ScrollViewer>
        <TextBlock x:Name="placeholder">Editor:</TextBlock>
    </ScrollViewer>
    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="1" Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <oldtoolkit:MarkdownTextBlock HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="MarkBlock" Background="Transparent" Text="{x:Bind MarkEditor.Text, Mode=OneWay}"/>
        </ScrollViewer>
        <Grid Grid.Column="2" Margin="15">
            <TextBox AcceptsReturn="True" x:Name="MarkEditor" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
        <toolkit:GridSplitter
           Grid.Column="1"
           Margin="0,0,-10,0"
           VerticalAlignment="Center" HorizontalAlignment="Right">
        </toolkit:GridSplitter>
    </Grid>-->
</Page>
