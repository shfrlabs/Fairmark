<Page
    x:Class="Fairmark.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fairmark"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    xmlns:models="using:Fairmark.Models"
    controls:BackdropMaterial.ApplyToRootOrPageBackground="True"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.Resources>
            <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Rectangle Fill="Transparent" x:Name="DragRegion" HorizontalAlignment="Stretch" Canvas.ZIndex="-1"/>
        <Grid x:Name="AppTitleBar" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" Margin="5,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <Image Source="Assets/StoreLogo.png" Width="15" Height="15" VerticalAlignment="Center" Margin="15,0,0,0"/>
                <TextBlock Text="Fairmark" FontSize="12" Margin="15,2,0,0" FontFamily="Segoe UI Variable Display"/>
                <Grid CornerRadius="2" Background="{StaticResource SystemAccentColorLight2}" Margin="8,0,0,0" Visibility="Collapsed" Loaded="DebugLoaded" VerticalAlignment="Center">
                    <TextBlock Text="DEBUG" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10" Padding="2,0" FontWeight="Bold" FontFamily="Segoe UI Variable"/>
                </Grid>
            </StackPanel>
            <CommandBar x:Name="MainCommands" VerticalAlignment="Center" Grid.Column="1" Margin="3,0,0,0" OverflowButtonVisibility="Collapsed" DefaultLabelPosition="Collapsed">
                <AppBarToggleButton Label="Files" Width="50" x:Name="Explorer" Click="Explorer_Click">
                    <AppBarToggleButton.Icon>
                        <FontIcon Glyph="&#xEC50;" />
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
                <AppBarToggleButton Icon="Find" Label="Search" Width="50" x:Name="Search" Click="Search_Click"/>
            </CommandBar>
            <controls:TabView
                x:Name="TabView"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                SelectionChanged="TabView_SelectionChanged"
                IsAddTabButtonVisible="False"
                TabCloseRequested="TabView_TabCloseRequested"
                CanDrag="False"
                CanDragTabs="False"
                CanReorderTabs="False"
                Margin="10,0,0,0">
            </controls:TabView>
        </Grid>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1" x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid CornerRadius="4" Margin="5" Padding="5,0" Grid.Column="0" Grid.RowSpan="2" Background="{ThemeResource LayerFillColorDefaultBrush}" x:Name="SideGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <AppBarButton Icon="ClosePane" Label="Close" Click="ClosePane_Click" LabelPosition="Collapsed" Width="50"/>
                <Grid Background="Green" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TreeView x:Name="Tree">
                        <TreeView.ItemTemplate>
                            <DataTemplate x:DataType="models:TreeNode">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind Emoji, Mode=OneWay}" />
                                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                                </StackPanel>
                            </DataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
                <Grid Background="Red" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock>search</TextBlock>
                </Grid>
            </Grid>
            <Frame x:Name="contentFrame" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Loaded="contentFrame_Loaded"/>
        </Grid>
    </Grid>
</Page>
